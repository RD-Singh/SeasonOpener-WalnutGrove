#include "main.h"
#include "definitions.h"
/*
using namespace pros::literals;

pros::Motor rightF(11, HIGHSPEED, N_REV, COUNTS);
pros::Motor rightB(13, HIGHSPEED, REV, COUNTS);
pros::Motor leftF(1, HIGHSPEED, REV, COUNTS);
pros::Motor leftB(2, HIGHSPEED, N_REV, COUNTS);

pros::Motor intakeM(10, TORQUE, REV, DEGREES);
pros::Motor liftL(3, HIGHSPEED, REV, DEGREES);
pros::Motor liftR(9, HIGHSPEED, N_REV, DEGREES);
pros::Motor scorer(14, TORQUE, N_REV, DEGREES);
*/

//pros::Controller master (MAIN);
